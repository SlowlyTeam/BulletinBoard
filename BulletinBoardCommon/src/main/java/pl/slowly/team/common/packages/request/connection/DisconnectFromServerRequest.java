package pl.slowly.team.common.packages.request.connection;

import pl.slowly.team.common.packages.request.Request;

/**
 * Sent when client wants to disconnect from server.
 */
public class DisconnectFromServerRequest extends Request {
    // tutaj dane potrzebene do identyfikacji polaczenia
}
